# Agent Rules

## Shared Rules
- Site capped at 12 sections. No new sections unless replacing a weaker one.
- MOBILE FIRST. Most users are on phones. All improvements should target mobile.
- No desktop-only features (hover effects, cursor interactions). Desktop is done.
- If unsure about a decision, text user via iMessage (chat_id 'any;-;+14847162152') AND add to QUESTIONS.md. Don't block.
- Only read the LAST 20 lines of CHANGELOG.md, not the whole file.
- Use targeted grep/read instead of reading entire files when possible.
- Get diff stats after pushing: /usr/local/bin/gh api repos/zed0minat0r/social-media-site/commits/HEAD --jq '.stats'
- ALWAYS text user via iMessage after completing work.
